* En la Tabla Territorio, se considerara al Atributo Terrirorio Padre
Solo como FK

* ¿Se puede usar SERIAL para autoincrementar los pk's?
CREATE SEQUENCE table_name_id_seq;

CREATE TABLE table_name (
    id integer NOT NULL DEFAULT nextval('table_name_id_seq')
);

ALTER SEQUENCE table_name_id_seq
OWNED BY table_name.id;

* ¿Colocar Tabla Concepto facturable para que no moleste al ingresar datos?

* Como Relacionar Precio con contrato 
